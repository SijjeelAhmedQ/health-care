fastapi>=0.115
uvicorn[standard]>=0.30
numpy>=1.24
httpx>=0.27
# ONNX Runtime with its CUDA provider (it also runs on the CPU): Silero VAD (python/models/silero_vad.onnx)
# and NVIDIA Parakeet-TDT 0.6B v2 (Configuration → Speech recognition → device GPU / CPU).
onnxruntime-gpu[cuda,cudnn]==1.23.2
# The CUDA / cuDNN libraries must not be newer than the NVIDIA driver supports (nvidia-smi → "CUDA Version").
# Pinned for a driver with CUDA 12.5 (555.x): with 12.9 builds cuDNN fails on the GPU ("CUDNN_BACKEND_API_FAILED").
# With a driver for CUDA 12.9 or newer these pins can be dropped.
nvidia-cuda-runtime-cu12==12.5.*
nvidia-cuda-nvrtc-cu12==12.5.*
nvidia-cublas-cu12==12.5.*
nvidia-nvjitlink-cu12==12.5.*
nvidia-cufft-cu12==11.2.3.61
nvidia-curand-cu12==10.3.6.82
nvidia-cudnn-cu12==9.3.0.75
# NVIDIA Parakeet-TDT 0.6B v2 as ONNX (downloaded into python/models on first use)
onnx-asr[hub]==0.12.0
# Omi Med STT v1 runtime (Parakeet-TDT 0.6B). Downloads the right artifact:
#   Linux/Windows CPU -> omi-health/omi-med-stt-v1-gguf   (then run: omi-med-stt install-cpp --cpp-backend cpu)
#   Apple Silicon     -> pip install -U "omi-med-stt[mlx]" -> omi-health/omi-med-stt-v1-mlx-q8 / -mlx
omi-med-stt>=0.2.1
# Second recogniser: re-hears each finished utterance with the app's names as vocabulary (Configuration → Speech recognition).
faster-whisper>=1.2
